---
name: yuexiang-hgkb1.0
description: 中国宏观经济仪表盘的周期性兜底更新（宏观看板周更）。固化"全量取数 → 逐项比对 → 条件更新 → 交付归档"四阶段管线：从 NeoData 单一数据源拉取 21 项宏观指标与事件日历，与既有仪表盘 HTML 比对，补漏发布日未入库的数据、刷新市场指标（10年国债收益率、美元兑人民币中间价）、滚动事件日历，仅在有实质变化时更新单文件离线 HTML（红升绿降配色）。触发词：宏观仪表盘、宏观看板、周一兜底更新、宏观数据更新、仪表盘更新、macro dashboard、补漏更新。
agent_created: true
---

# 中国宏观经济仪表盘 · 兜底更新管线（方案A：每周一 10:00）

本技能用于对既有「中国宏观经济仪表盘」（单文件离线 HTML）执行周期性兜底更新：补漏、刷新市场指标、滚动事件日历。全部数据仅来自 **NeoData 金融搜索**（`neodata-financial-search` 技能），禁止混用任何其他数据源，禁止用训练数据或推测填补任何缺口。

**本技能自带管线模板**（`assets/`），首次使用可自动初始化仪表盘，无需手工搭建。

## 部署前提（其他用户必读）

- 必须已安装 `neodata-financial-search` 技能（唯一数据源）；
- 必须有 NeoData 金融数据服务的访问凭证（经 `connect_cloud_service` 获取）；
- 无上述条件时，本技能无法取数，仪表盘无法建立或更新。

## 适用场景

- 每周一兜底更新（主场景）：补漏发布日未入库的数据、刷新国债/汇率、滚动事件日历。
- 数据发布日触发的更新：同一工作流，侧重当日新发布数据的入库比对。
- 用户手动要求"更新宏观仪表盘 / 宏观看板"。
- **首次使用**：工作区尚无仪表盘与管线 → 走 Step -1 初始化（见下）。

## 前置定位

仪表盘与管线位于当前工作区（workspace 根目录）：

- 仪表盘：`outputs/中国宏观经济仪表盘.html`
- 取数脚本：`outputs/pipeline/dashboard_runner.py`
- 查询契约：`outputs/pipeline/queries.json`（21 条查询，勿随意增删）
- 快照目录：`outputs/pipeline/data/YYYY-MM-DD_HHMM/`
- 执行记录：`.workbuddy/memory/automations/<本技能对应自动化ID>/memory.md`（先读历史再执行；若无该自动化则为手动触发，跳过此步）

## 执行流程（严格按序）

### Step -1 · 首次初始化（仅当工作区无管线时执行）

检测 `outputs/pipeline/dashboard_runner.py` 是否存在：

- **存在** → 跳过本步，直接进入 Step 0/1；
- **不存在** → 从技能内置模板初始化：
  1. 复制 `assets/pipeline/dashboard_runner.py` → `outputs/pipeline/dashboard_runner.py`；
  2. 复制 `assets/pipeline/queries.json` → `outputs/pipeline/queries.json`；
  3. 复制 `assets/中国宏观经济仪表盘_template.html` → `outputs/中国宏观经济仪表盘.html`；
  4. 告知用户"已从技能模板初始化管线，仪表盘当前为模板数据，即将跑首轮更新刷新为实时数据"；
  5. 继续执行 Step 1 全量取数（首轮更新会把模板里的旧数据全部刷新）。

初始化失败（如凭证不可用）时，明确告知用户缺什么，不生成空仪表盘。
若文件复制成功但首轮取数失败，仪表盘页头须保留模板数据时点并明确提示"模板数据，首轮取数失败，待数据服务恢复后刷新"，不得让旧数据冒充实时数据。

### Step 0 · 读执行记录
读取该自动化的 `memory.md`（若存在），了解上次执行结果与遗留事项。

### Step 1 · 全量取数
```bash
python outputs/pipeline/dashboard_runner.py
```
- 输出每条查询 `[ok]/[empty]` 状态，并生成快照目录（`snapshot.json` + 各 `raw_*.json`）。
- **凭证失效处理（周一上午高发）**：若脚本输出空结果且手动单条查询返回 `TOKEN_EXPIRED`/`TOKEN_MISSING`，或返回 401/403/40101：
  1. 调用 `connect_cloud_service`（无参数）获取凭证，优先取 `tempToken` 字段；
  2. `python <neodata技能目录>/scripts/query.py --save-token "<凭证>"` 保存（凭证禁止在回复中展示）；
  3. 重跑 `dashboard_runner.py`。最多重试 1 次，两次失败则告知"金融数据服务暂时不可用"并停止。

### Step 2 · 逐项比对（核心判断）
对照 `references/indicators-and-gaps.md` 的比对清单，将快照数据与仪表盘现值逐项比对，识别四类实质变化：

1. **补漏**：此前标注"未召回/未发布/待发布"的指标，本期已入库 → 更新（结构化 API 未入库的，换资讯通道措辞重试，见 Step 2b）。
2. **刷新市场指标**：10 年国债收益率、美元兑人民币中间价取最新日频值；LPR 若逢 20 日报价日则核对。
3. **滚动事件日历**：已过期事件行移入"已发布"区并回填现值（前值/预测/现值齐全）；新增未来 1–2 周焦点事件（中国 CPI/PPI/PMI/金融数据发布日、美国非农/ISM/CPI/PCE、FOMC、LPR 报价日等），日期以 NeoData 资讯召回为准，惯例日期标注"惯例"。
4. **数据时点**：页头"数据时点"、页脚"更新于"、经济温度结论横幅同步刷新。

#### Step 2b · 发布日数据走资讯通道
**关键经验：数据发布当天，结构化 API 通常尚未入库**。当天发布的数据（如月末最后一天发布的当月 PMI）必须改用资讯通道查询，措辞带发布主体与日期，例如：
```
2026年8月中国制造业PMI 统计局8月31日发布 非制造业商务活动指数
```
若结构化与资讯通道均未召回，该指标标注"未召回"，不得臆测填补。

### Step 3 · 条件更新
- **有实质变化** → 更新仪表盘 HTML：
  - 保持**红升绿降**配色（`--up:#c92a2a` 回升、`--down:#148a5c` 回落）与中国惯例；
  - 保持单文件离线格式（无外链依赖，图表为内联 SVG）；
  - 卡片、趋势图、事件日历、复现查询清单、口径说明与风险提示五个板块联动更新（复现清单新增本期使用的查询语句与取回结果）；
  - 更新后用 Python HTMLParser 校验标签闭合。
- **完全无实质变化** → 不修改文件，仅在当日 `.workbuddy/memory/YYYY-MM-DD.md` 记录"周一兜底：无实质变化"，静默退出，不调用 present_files。

### Step 4 · 交付与归档
- 有更新：`present_files` 交付仪表盘；摘要列明本期实质变化清单。
- 将高层执行摘要（变化清单、凭证处理、缺口情况）追加到该自动化的 `memory.md`，勿写全文。
- 在当日 `.workbuddy/memory/YYYY-MM-DD.md` 追加执行记录。

## 铁律（任何情况不得违反）

1. **单一数据源**：只用 NeoData；缺口标注"未召回"，禁止臆测填补、禁止用训练数据补数。
2. **长期已知缺口不重试**：见 `references/indicators-and-gaps.md` 的缺口清单；新增缺口连续 3 轮未召回后转入该清单。
3. **无实质变化不改文件**：避免无意义改动污染历史。
4. **凭证静默处理**：任何场合不向用户展示、复述凭证内容。
5. **事件日历不臆造日期**：未在 NeoData 资讯召回中出现的日期一律标注"惯例"或留空。
